/**
 * Injection token for Crud Config configuration properties.
 * Used to provide and inject configuration options throughout the module.
 */
export const CRUD_CONFIG_PROPERTIES: string = "CRUD_CONFIG_PROPERTIES";

/**
 * Injection token for Config entries.
 * Used to provide and inject the configuration entries from the database.
 */
export const CRUD_CONFIG_ENTRIES: string = "CRUD_CONFIG_ENTRIES";
