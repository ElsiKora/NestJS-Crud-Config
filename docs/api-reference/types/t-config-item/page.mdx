import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `TConfigItem`

Type alias representing a map of config name/value pairs inside a section.

_Defined in: `src/modules/config/type/item.type.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import type { TConfigItem } from "@elsikora/nestjs-crud-config";
export default TConfigItem`,
 })}
/>
