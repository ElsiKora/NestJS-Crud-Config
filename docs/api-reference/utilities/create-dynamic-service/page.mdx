import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `createDynamicService`

Factory function to create dynamic service classes for generated entities.

*Defined in: `src/shared/utility/create-dynamic/service.utility.ts`*

<TSDoc
	definition={generateDefinition({
		code: `import { createDynamicService } from "@elsikora/nestjs-crud-config";
export default createDynamicService`,
	})}
/>


