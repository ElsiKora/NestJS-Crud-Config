import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `createDynamicEntityClass`

Factory function to create TypeORM entity classes at runtime.

_Defined in: `src/shared/utility/create-dynamic/entity.utility.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import { createDynamicEntityClass } from "@elsikora/nestjs-crud-config";
export default createDynamicEntityClass`,
 })}
/>
