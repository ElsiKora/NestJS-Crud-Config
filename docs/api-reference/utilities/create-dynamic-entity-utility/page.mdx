import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `createDynamicEntityUtility`

Alias export for `createDynamicEntityClass`.

_Defined in: `src/shared/utility/index.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import { createDynamicEntityUtility } from "@elsikora/nestjs-crud-config";
export default createDynamicEntityUtility`,
 })}
/>
