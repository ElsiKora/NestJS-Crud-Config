import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigCacheOptions`

Cache configuration options.

_Defined in: `src/shared/interface/config/cache-options.interface.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import type { IConfigCacheOptions } from "@elsikora/nestjs-crud-config";
export default IConfigCacheOptions`,
 })}
/>
