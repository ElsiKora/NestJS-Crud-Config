import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigEncryptionOptions`

Encryption configuration options.

*Defined in: `src/shared/interface/config/encryption-options.interface.ts`*

<TSDoc
	definition={generateDefinition({
		code: `import type { IConfigEncryptionOptions } from "@elsikora/nestjs-crud-config";
export default IConfigEncryptionOptions`,
	})}
/>


