import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigGetOptions`

Options for reading a single config value.

*Defined in: `src/modules/config/interface/get-options.interface.ts`*

<TSDoc
	definition={generateDefinition({
		code: `import type { IConfigGetOptions } from "@elsikora/nestjs-crud-config";
export default IConfigGetOptions`,
	})}
/>


