import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigControllersOptions`

Controls dynamic controllers generation (enable/disable and controller properties).

_Defined in: `src/shared/interface/config/controller-options.interface.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import type { IConfigControllersOptions } from "@elsikora/nestjs-crud-config";
export default IConfigControllersOptions`,
 })}
/>
