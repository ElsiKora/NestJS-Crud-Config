import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigOptions`

Core configuration options for `CrudConfigModule.register()` and the async factory in `registerAsync()`.

_Defined in: `src/shared/interface/config/options.interface.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import type { IConfigOptions } from "@elsikora/nestjs-crud-config";
export default IConfigOptions`,
 })}
/>
