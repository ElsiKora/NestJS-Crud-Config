import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigSetOptions`

Options for creating/updating a config value.

*Defined in: `src/modules/config/interface/set-options.interface.ts`*

<TSDoc
	definition={generateDefinition({
		code: `import type { IConfigSetOptions } from "@elsikora/nestjs-crud-config";
export default IConfigSetOptions`,
	})}
/>


