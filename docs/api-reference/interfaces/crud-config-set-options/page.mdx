import { generateDefinition, TSDoc } from "nextra/tsdoc";

# `IConfigSetOptions`

Options for creating/updating a config value.

`eventManager`, when provided, must be the exact manager already bound to an active Automator transaction. Omit it for a standalone `set()`, which owns the named `crud-config-set` transaction.

_Defined in: `src/modules/config/interface/set-options.interface.ts`_

<TSDoc
 definition={generateDefinition({
  code: `import type { IConfigSetOptions } from "@elsikora/nestjs-crud-config";
export default IConfigSetOptions`,
 })}
/>
